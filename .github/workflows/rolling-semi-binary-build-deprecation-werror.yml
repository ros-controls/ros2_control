name: Rolling Semi-Binary Build Deprecation Werror
# description: 'Build with deprecation warnings treated as errors.'

on:
  workflow_dispatch:
  pull_request: &event
    branches:
      - master
      - '*feature*'
      - '*feature/**'
    paths:
      - '**.hpp'
      - '**.h'
      - '**.cpp'
      - '**.py'
      - '**.yaml'
      - '.github/workflows/rolling-semi-binary-build-deprecation-werror.yml'
      - '**/package.xml'
      - '**/CMakeLists.txt'
      - 'ros2_control-not-released.rolling.repos'
      - 'ros2_control-not-released.lyrical.repos'
      - '**.xml'

concurrency:
  # cancel previous runs of the same workflow, except for pushes on given branches
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/heads') }}

jobs:
  semi_binary_deprecation_warnings_as_errors:
    uses: ros-controls/ros2_control_ci/.github/workflows/reusable-industrial-ci-with-cache.yml@master
    strategy:
      fail-fast: false
      matrix:
        ROS_DISTRO: [lyrical, rolling]
    with:
      ros_distro: ${{ matrix.ROS_DISTRO }}
      ros_repo: testing
      upstream_workspace: ros2_control.${{ matrix.ROS_DISTRO }}.repos
      not_test_build: true
      target_cmake_args: -DCMAKE_CXX_FLAGS="-isystem /opt/ros/${{ matrix.ROS_DISTRO }}/include -Werror=deprecated-declarations"
