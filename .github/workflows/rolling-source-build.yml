name: Rolling Source Build
on:
  workflow_dispatch:
  pull_request:
    branches:
      - master

jobs:
  source:
    uses: ros-controls/ros2_control_ci/.github/workflows/reusable-ros-tooling-source-build.yml@fix/source
    with:
      ros_distro: rolling
      ref: master
      ros2_repo_branch: master
      container: ubuntu:24.04
